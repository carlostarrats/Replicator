{
  "name": "vercel-config-manager",
  "version": "0.1.0",
  "description": "Replicator, a Vercel config manager for safe project analysis, drift checks, and migration handoff workflows.",
  "private": false,
  "license": "MIT",
  "type": "module",
  "bin": {
    "vcopy": "./src/cli.mjs"
  },
  "scripts": {
    "dogfood": "node scripts/dogfood-local.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
